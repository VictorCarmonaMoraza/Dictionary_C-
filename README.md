# 2 - Se agrega la forma segura de agregar elementos a un diccionario

 Console.WriteLine("Forma 3-->Agregando elemento de forma segura");
            if (!users.ContainsKey("Lucia"))
            {
                users.Add("Lucia", 109);
            }
            }
